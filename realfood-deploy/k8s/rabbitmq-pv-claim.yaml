apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rabbitmq-pv-claim
  labels:
    app: rabbitmq
spec:
  storageClassName: "rabbitmq-sc"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi